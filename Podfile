# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'QuizAppDynamox' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuizAppDynamox
  pod 'Alamofire', '~> 5.6'
  pod 'RealmSwift', '~> 10.0'
  pod 'CombineExt', '~> 1.0'

  target 'QuizAppDynamoxTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble', '~> 10.0'
    pod 'Quick', '~> 5.0'
  end

  target 'QuizAppDynamoxUITests' do
    # Pods for testing
  end

end
